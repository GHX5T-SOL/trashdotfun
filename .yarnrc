# Yarn configuration to avoid platform-specific package issues
# This prevents yarn from trying to install non-existent platform variants

# Disable optional dependencies that might cause issues
optional false

# Set platform to current system
platform "$(uname -s)-$(uname -m)"

# Ignore platform-specific optional dependencies
ignore-optional true

# Use exact versions to avoid dependency resolution issues
exact true
